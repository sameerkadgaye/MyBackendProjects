package lamda_Expression_concepts;

public class IntroductionOfLamdbaExpClass {
//Lambda Takes Consumer void java.util.function.Consumer.accept(T t)
/*
       *** JAVA LAMBDA EXPRESSION  ***

       BASIC SYNTAX :- (argument-list) -> {body}

            Java lambda expression is consisted of three components.
                1) Argument-list: It can be empty or non-empty as well.
                2) Arrow-token: It is used to link arguments-list and body of expression.
                3) Body: It contains expressions and statements for lambda expression.

        : No Parameter Syntax :-
            () -> {
             //Body of no parameter lambda
            }

        : One Parameter Syntax :-
            (p1) -> {
             //Body of single parameter lambda
            }

        : Two Parameter Syntax :-
            (p1,p2) -> {
            //Body of multiple parameter lambda
            }


    : IMPORTANT RULES FOR LAMBDA EXPRESSION :-

        1. Jar Lambda Expression chya Body Madhe 1ch statement (only one line code) asel.
            tar curly braces Lamdba Expression sobat den he optional ahe.
            
            Ani Single Line Statement chy case madhe return keyword den he sudhha optional ahe return keyword 
            Na deta direct value return karu sakto.

        2. Lambda Expression sobat Java Compiler ha swatah enter karat asto variable
           chya type la jo passed asel arguments madhe. manun Lambda Expression Sobat
           Type den he Optional aste.
           example : -  : With Type Syntax:
                   (int p1, float p2) -> {
                        //Body of multiple parameter lambda
                    }

                  : Without Type Syntax (As per Discussed In 2nd Rule Point):
                   (p1, p2) -> {
                        //Body of multiple parameter lambda
                    }
*/
}
